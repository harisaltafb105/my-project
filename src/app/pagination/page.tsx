import React from 'react';
import PaginationPage from '../Components/PaginationPage'; // Import PaginationPage

const Page = () => { // Use a capital "P" for the component name
  return (
    <div>
      <PaginationPage /> {/* Render the PaginationPage component */}
    </div>
  );
};

export default Page; // Export the Page component as default
